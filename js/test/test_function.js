function diff_num(n) { return 1; }

function diff_num(n) {
	return 1;
}

function diff_num(n)
{
	if (n <= 19)
	{
		return true;
	}
	else
	{
		return (n-10)*3;
	}
}

function diff_num(a,b)
{
	for (let i = a; i < b; i++)
		a++;
}

function diff_num(a,b,c)
{
	switch (a)
	{
	case b:
		let y = 2;
	case c:
		let z = 3;
		break;
	default:
		x = 5;
	}
}

a => a + 5;

(a) => a + 3;

(a,b) => {
	if (a < b)
		return b + 3;
	return a + 3;
}